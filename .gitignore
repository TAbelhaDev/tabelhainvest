node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test
!.env.development
!.env.production

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Wrangler
.dev.vars

# Claude Code local state (agent worktrees etc.)
/.claude/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# scratch de debug
tmp-*

# Config de rede local (esta máquina usa mirror npmmirror porque o
# registry.npmjs.org resolve só IPv6 aqui). Não commitar — o CI/contribuidores
# devem usar o registry padrão.
bunfig.toml
